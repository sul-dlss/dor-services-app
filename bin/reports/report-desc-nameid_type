#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative '../../config/environment'
require_relative '../../lib/unique_report'

UniqueReport.new(name: 'desc-nameid_type', dsids: ['descMetadata']).run do |ng_xml|
  ng_xml.xpath('//mods:nameIdentifier/@type', mods: MODS_NS).map(&:content)
end
