---
request:
  filter_keys:
    - 'HTTP_AUTHORIZATION'
    - 'HTTP_X_AUTH'
exceptions:
  ignore:
    - 'ReindexJob::DeadLockError'
stage:
  exceptions:
    ignore:
      - 'PreservationIngestService::VersionMismatchError'      
insights:
  enabled: false
