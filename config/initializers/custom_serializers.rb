# frozen_string_literal: true
# Add our cocina-models serializer (for ActiveJob)
# Uncomment this when we upgrade to Rails 6, and then we don't need to manually use the serizizer,
# as Rails supports it automatically.
# Rails.application.config.active_job.custom_serializers << Cocina::Serializer
